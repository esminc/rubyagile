<h1>Welcome#index</h1>
<p>Find me in app/views/welcome/index.html.erb</p>
<p>
<% if session[:user_id] -%>
<%= User.find_by_id(session[:user_id]).login %> <%= link_to('Log out', {:controller => 'sessions', :action => 'destroy'}) %>
<% else -%>
<%= link_to('Log in', {:controller => 'sessions', :action => 'new'}) %>
<% end -%>
</p>
